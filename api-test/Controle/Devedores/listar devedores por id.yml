info:
  name: listar devedores por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/devedores/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
