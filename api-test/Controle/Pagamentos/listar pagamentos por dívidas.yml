info:
  name: listar pagamentos por dívidas
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/pagamentos?dividaIds=1,2"
  params:
    - name: dividaIds
      value: 1,2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
