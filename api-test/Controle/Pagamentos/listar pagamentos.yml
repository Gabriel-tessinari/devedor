info:
  name: listar pagamentos
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/pagamentos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
