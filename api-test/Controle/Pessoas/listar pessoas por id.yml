info:
  name: listar pessoas por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/pessoas/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
