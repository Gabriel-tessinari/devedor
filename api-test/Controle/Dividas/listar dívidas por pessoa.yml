info:
  name: listar dívidas por pessoa
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/dividas/devedor/20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
