info:
  name: listar dívidas por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/dividas/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
